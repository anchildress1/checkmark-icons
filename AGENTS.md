# AI Instructions

- Prefer ES6 module JS with arrow style functions
- Always code with YAGNI & KISS principles
- Comments belong in JSDocs, not inline
- Run the format, then lint, then all tests with coverage to validate every change
